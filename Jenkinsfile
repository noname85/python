pipeline {
    agent none
    stages {
        stage('build') {
            steps {
                sh 'date'
                sh 'pwd'
                sh 'python holaJenkins.py'
                sh 'hostname'
            }
        }
  
    }
}
